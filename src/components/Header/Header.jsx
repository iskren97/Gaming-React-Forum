import React from 'react';
import './Header.css';

const Header = () => {
  return (
    <header>
      <h1>This will be the header, holding navbar, search-bar etc.</h1>
    </header>
  );
};

export default Header;
